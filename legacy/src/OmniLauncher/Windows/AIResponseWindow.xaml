<Window x:Class="OmniLauncher.Windows.AIResponseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OmniLauncher AI"
        Width="640" Height="480"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">
    <Border Background="#1E1E2E" CornerRadius="12"
            BorderBrush="#45475A" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="🤖 OmniLLM Response" Foreground="#CBA6F7" FontSize="14" FontWeight="Bold"/>
                <TextBlock x:Name="PromptLabel" Foreground="#7F849C" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Response -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="ResponseText"
                         Background="Transparent" Foreground="#CDD6F4"
                         BorderThickness="0" IsReadOnly="True"
                         TextWrapping="Wrap" FontSize="13"
                         VerticalAlignment="Top"/>
            </ScrollViewer>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="CopyBtn" Content="Copy" Width="80" Height="32" Margin="0,0,8,0"
                        Background="#313244" Foreground="#CDD6F4" BorderThickness="0"
                        Cursor="Hand" Click="CopyBtn_Click"/>
                <Button Content="Close" Width="80" Height="32"
                        Background="#45475A" Foreground="#CDD6F4" BorderThickness="0"
                        Cursor="Hand" Click="CloseBtn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>