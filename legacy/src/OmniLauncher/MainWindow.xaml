<Window x:Class="OmniLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OmniLauncher"
        Width="680" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Border Background="{StaticResource WindowBackground}"
            CornerRadius="12"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="12,12,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search Box -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBox x:Name="SearchBox"
                         Style="{StaticResource SearchBoxStyle}"
                         TextChanged="SearchBox_TextChanged"
                         KeyDown="SearchBox_KeyDown"
                         PreviewKeyDown="SearchBox_PreviewKeyDown"/>
                <TextBlock x:Name="Placeholder"
                           Text="Search apps, files, web..."
                           Style="{StaticResource PlaceholderStyle}"
                           IsHitTestVisible="False"/>
            </Grid>

            <!-- Results List -->
            <ListView x:Name="ResultsList"
                      Grid.Row="1"
                      Style="{StaticResource ResultsListStyle}"
                      ItemContainerStyle="{StaticResource ResultItemStyle}"
                      MouseDoubleClick="ResultsList_MouseDoubleClick"
                      KeyDown="ResultsList_KeyDown"
                      Visibility="{Binding HasItems, RelativeSource={RelativeSource Self}, Converter={StaticResource BoolToVis}}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="48">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="{Binding IcoPath}" Width="24" Height="24"
                                   Margin="8,0,8,0" VerticalAlignment="Center"
                                   Visibility="{Binding IcoPath, Converter={StaticResource BoolToVis}}"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" Style="{StaticResource ResultTitleStyle}"/>
                                <TextBlock Text="{Binding SubTitle}" Style="{StaticResource ResultSubTitleStyle}"
                                           Visibility="{Binding SubTitle, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Bottom padding -->
            <Border Grid.Row="1" Height="8"/>
        </Grid>
    </Border>
</Window>